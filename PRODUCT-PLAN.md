# 多语言文案本地化 Agent 项目 · 产品侧设计规划

> 版本 v1.0 · 用于产品侧 ↔ 研发侧对接 · 承接研发 2–3 人
> 关联设计:`localization-agents/`(两个 Agent Team 的 system prompt + skill + 示例数据,已完成)

---

## 0. 一页速览(TL;DR)

- **做什么**:把"中文功能文案 → 多语言 UI 文案"的 UX Writer 环节与母语走查环节,做成两个协作的 Agent Team,替代/加速现有人工 + 外包供应商流程。
- **产品侧负责**:① Agent 的 system prompt 设计 ② skill 文档 ③ 评测集(黄金集)④ 护栏与风险分级策略 ⑤ I/O 契约与指标定义 ⑥ 分阶段验收。
- **研发侧负责**:编排引擎、系统集成(Starling/网关)、结构化输出校验、程序化护栏、可观测(Langfuse)、成本/性能、可靠性、灰度发布、数据服务化。
- **交付节奏**:P0 原型 → P1 影子模式 → P2 灰度 → P3 全量,每阶段用指标门禁卡口。
- **核心成功标准**:首轮通过率、术语一致率、母语走查返工率、单条成本与时延、人工介入率 —— 见 §7。

---

## 1. 项目背景与目标

### 1.1 现状痛点(对应现有流程图)
| 环节 | 现状 | 问题 |
|---|---|---|
| ② UX Writer:审中文 + 写英文 | 人工,约 **1 周** | 人力瓶颈、速度慢 |
| ⑤ 英译多 | 外部供应商(RWS / 中电金信),约 **0.5 周** | 外包成本、周期、协同摩擦 |
| ⑦ 母语走查 | 人工,问题双向回退 | 返工链路长,反馈不沉淀 |

### 1.2 项目目标
1. **提效**:UX Writer 从"撰写"转为"审校",单条产出从周级降到分钟级初稿 + 人工终审。
2. **降本**:英译多由 AI 出初稿,供应商转为 post-editing 或仅保留高风险文案。
3. **质量可控**:用评测集 + 护栏 + 可观测,让质量可度量、可追溯、可回退。
4. **资产沉淀**:走查结论回流术语库/翻译记忆,系统越用越准(自研相对外包的核心价值)。

### 1.3 非目标(本期不做)
- 不做全自动无人化(高风险文案始终人工把关)。
- 不替换 Starling / 供应商体系,而是嵌入其中。
- 不做图片/视频内文案的 OCR 本地化(后续迭代)。

---

## 2. 项目范围与方案总览

### 2.1 两个 Agent Team(设计已完成,见 `localization-agents/`)

**Team A · UX Writer 团队**(对应②+⑤:审中文 + 写英文 + 英译多)
`ux-writer-lead`(编排) · `source-copy-reviewer` · `en-copywriter` · `multilingual-localizer` · `constraint-validator`

**Team B · 审校团队**(对应⑦:母语走查/验证)
`review-lead`(编排) · `native-linguistic-reviewer` · `terminology-consistency-checker` · `cultural-compliance-checker` · `visual-context-checker` · `verdict-aggregator`

### 2.2 端到端流程
```
PM 提交单(中文+PRD+UI图+字符限制+key)
   → Team A:审中文 → 写英文(SoT) → 逐语言创译(并行) → 约束校验
   → 人工 Signoff → 上传 Starling
   → Team B:语言/术语/文化/视觉 四维走查 → 终裁 + 问题分类
        · 中英文问题 → 回退 Team A
        · 多语言问题 → 回退对应语言 localizer
   → 通过项回流 TM/术语库(越用越准)
```
> 完整原则与契约见 `localization-agents/README.md` 及各 team 的 README。

---

## 3. 职责划分(产品侧 ↔ 研发侧)

**一句话分工:产品侧定义"什么是对的",研发侧保证"把它做稳、做快、可上线"。**

| 能力域 | 产品侧 | 研发侧 |
|---|---|---|
| Agent system prompt | **主导设计与迭代** | 落地、版本化托管 |
| Skill 文档 | **主导编写** | 服务化(术语/记忆接后端) |
| 评测集(黄金集) | **主导建立与维护** | 提供评估平台/自动化 |
| I/O 契约 | **定义字段与语义** | 转 JSON Schema + 强制校验 |
| 护栏与风险分级 | **定策略(哪些走人工)** | 工程实现(校验/审核/注入防护) |
| 成功指标 | **定义与验收** | 埋点、看板 |
| 编排/集成/可靠性/成本/灰度/安全 | 提需求、定 SLA | **主导实现** |

> RACI 详版见 §8。

---

## 4. 产品侧交付物(本规划的核心)

### 4.1 System Prompt(✅ 已完成)
11 个 agent 的 system prompt,含角色、输入、流程、输出契约、护栏原则。
路径:`localization-agents/teams/*/agents/*.md`

### 4.2 Skill 文档(✅ 已完成)
6 个共享 skill:
- Team A:`glossary-and-tm`、`transcreation-guide`、`localization-constraints`
- Team B:`review-rubric`、`issue-taxonomy`、`feedback-loop`
路径:`localization-agents/teams/*/skills/*/SKILL.md`

### 4.3 评测集 / 黄金集(⚠️ 本期重点,产品侧主导新建)

**这是决定项目专业度的核心资产,产品侧必须亲自主导,不外包给研发。**

**a) 结构(每条评测样例)**
```json
{
  "id": "eval-0001",
  "key": "sku.button.sold_out",
  "zh": "已抢光",
  "control": "按钮",
  "tone": "克制",
  "scene": "详情页秒杀库存为0",
  "char_limit_en": 12,
  "vars": [],
  "target_langs": ["en","ja","ko"],
  "category": "commerce/button",
  "difficulty": "easy|medium|hard",
  "gold": { "en": "Sold Out", "ja": "完売", "ko": "품절" },
  "must_pass": ["长度合规","术语一致","占位符保留"],
  "expected_flags": []
}
```

**b) 覆盖分布矩阵(建议首批 ≥ 100 条,逐步扩到 200+)**
| 维度 | 需覆盖 |
|---|---|
| 控件类型 | 按钮 / Toast / 弹窗标题 / 弹窗正文 / 表单错误 / 占位符 / 菜单 / 营销 Banner |
| 难度 | easy(常见短词)/ medium(含变量·语气)/ hard(双关·营销·文化敏感) |
| 边界 | 超长、含 `{count}` 变量、复数、含术语库词、含不可翻译品牌词、RTL 语言 |
| 语言 | 至少覆盖首发目标语言(如 en/ja/ko/th/id + 1 个 RTL) |
| 陷阱 | 故意放"薯条→French Fries"类术语陷阱、注入攻击样本 |

**c) 评分维度**(复用 `review-rubric` skill 的 5 分制 + severity)
- 客观(代码判定):长度合规率、占位符保留率、术语命中率、格式合法率。
- 主观(LLM-as-judge + 人工校准):地道度、语气匹配、文化适配。

**d) 来源**:历史真实文案(优先)+ UX Writer 已确认译文作为 gold + 人工补造边界样例。
**e) 维护**:每次线上发现的 badcase 回灌评测集;评测集本身季度评审。

### 4.4 护栏与风险分级策略(产品侧定,研发实现)
| 风险级 | 内容类型 | 处置 |
|---|---|---|
| 高(强制人工) | 营销 slogan、法务、支付、隐私、健康、文化/宗教/政治敏感 | Agent 出稿 + **必须人工终审** |
| 中 | 含变量/复数、超长、命中术语但无历史 TM | Agent 出稿 + 抽检 |
| 低 | 常见短文案且 TM 命中 | 可自动放行(仍记录) |

### 4.5 I/O 契约(产品定语义 → 研发转 Schema)
现有各 agent 的"输出契约"即字段定义;研发据此产出正式 JSON Schema 并强制校验(见 §5 需求 R3)。

### 4.6 指标定义(见 §7)

---

## 5. 研发侧承接需求清单(给 2–3 位研发)

按优先级(P0 必做 / P1 应做 / P2 增强)列出,研发据此排期。

| # | 需求 | 优先级 | 说明 |
|---|---|---|---|
| R1 | **编排引擎** | P0 | 确定性执行 lead 的分派:重试/超时/并发 fan-out/失败降级/死信兜底 |
| R2 | **Starling 集成** | P0 | 读提交单、写回中英/多语言到对应 key;鉴权、限流 |
| R3 | **结构化输出校验** | P0 | 用 JSON Schema 强制 agent 输出,解析失败自动重试 |
| R4 | **程序化护栏** | P0 | 长度/占位符/复数用代码校验(不信 LLM 自评);输入防 prompt 注入;输出内容审核 |
| R5 | **术语库/TM 服务化** | P0 | 从 CSV/JSONL 升级为服务(DB/向量库),支持并发写、版本、仅 approved 复用 |
| R6 | **可观测(Langfuse)** | P0 | 全链路 tracing、每 agent 的 token/延迟/成本/失败率看板 |
| R7 | **评估平台** | P1 | 跑黄金集自动打分 + 上线回归门禁(产品提供评测集与标准) |
| R8 | **模型版本锁定 + prompt 版本化** | P1 | 可复现、可回滚 |
| R9 | **成本/性能优化** | P1 | 分级模型路由(校验用便宜模型)、prompt 缓存、TM 命中优先 |
| R10 | **灰度发布** | P1 | 影子/灰度/全量、A/B、feature flag |
| R11 | **人在环工具** | P1 | 审批队列 + 反馈回流入口(可复用 Starling 前端) |
| R12 | **安全/合规** | P0 | key 管理、**数据出境合规**、审计日志 |

---

## 6. 里程碑与排期(2–3 研发)

> 建议研发分工:**R-甲**=编排+集成(R1/R2/R5),**R-乙**=评估+可观测+成本(R6/R7/R9),**R-丙/共享**=护栏+Schema+HITL 前端(R3/R4/R11)。产品侧全程负责 prompt/skill/评测集/验收。

| 阶段 | 周期(估) | 研发做 | 产品做 | 出口门禁 |
|---|---|---|---|---|
| **P0 原型** | 2–3 周 | R1 R3 最小闭环,跑通单批 | 完成评测集 v1(≥100 条)、护栏策略定稿 | 黄金集自动跑通,结构化输出稳定 |
| **P1 影子模式** | 3–4 周 | R2 R4 R5 R6 R12 接入 Starling+Langfuse | 用真实文案对比"Agent 稿 vs 人工稿" | 影子指标达标(见 §7),不影响线上 |
| **P2 灰度** | 2–3 周 | R7 R8 R9 R10 R11 | 定灰度范围(低风险内容先行)、盯看板 | 灰度指标 ≥ 阈值,返工率可控 |
| **P3 全量** | 持续 | 稳定性/成本调优 | badcase 回灌评测集、季度评审 | 全量指标稳定,ROI 达成 |

---

## 7. 成功指标与验收门禁

| 指标 | 定义 | 目标(建议,上线前对齐) |
|---|---|---|
| 首轮通过率 | Team A 初稿经 Team B 走查一次通过占比 | ≥ 80% |
| 术语一致率 | 命中术语库译法的正确占比 | ≥ 98% |
| 长度合规率 | 不超字符上限占比 | ≥ 99% |
| 占位符保留率 | 变量正确保留占比 | 100%(硬指标) |
| 母语走查返工率 | 需打回重译占比 | ≤ 20% 且逐月下降 |
| 人工介入率 | 需人工终审占比 | 高风险 100%,整体逐步下降 |
| 单条成本 / 时延 | 每条 token 成本、端到端时延 | 低于人工+外包基线 |
| ROI | 节省工时+外包费 vs 模型成本 | 正向 |

> **门禁规则**:每阶段出口,黄金集自动评分 + 上述指标未达阈值,不进入下一阶段。

---

## 8. RACI(关键活动责任矩阵)

> R=负责执行 A=最终担责 C=被咨询 I=被告知 · PM=产品 / RD=研发 / Ling=语言专家

| 活动 | PM | RD | Ling |
|---|---|---|---|
| system prompt / skill 设计 | R/A | C | C |
| 评测集建立与维护 | R/A | C | R |
| 护栏与风险分级 | R/A | C | C |
| I/O 契约定义 | R/A | C | I |
| 编排/集成/校验/可观测/成本/灰度/安全 | C/I | R/A | I |
| 上线门禁评审 | R/A | R | C |

---

## 9. 风险与依赖

| 风险/依赖 | 影响 | 应对 |
|---|---|---|
| 评测集质量不足 | 无法判定好坏,项目失焦 | 产品侧优先投入,联合语言专家,首批≥100条 |
| 模型幻觉编造术语 | 术语错、品牌受损 | 程序化术语校验 + 审校团队拦截 + 回流 |
| PRD 文本 prompt 注入 | 绕过护栏 | 输入清洗/隔离(R4) |
| 数据出境合规 | red line | 合规前置评估(R12),必要时区域化部署 |
| Starling/网关能力 | 集成阻塞 | P0 先确认 API 能力与配额 |
| 高风险文案误放行 | 线上事故 | 风险分级强制人工(§4.4) |

---

## 10. 附录 · 文件索引

```
localization-agents/
├── README.md                      # 总体设计 + Agent Teams 原则 + 协作流
├── PRODUCT-PLAN.md                # 本文档
├── teams/
│   ├── ux-writer/  (Team A)       # README + 5 agents + 3 skills
│   └── reviewer/   (Team B)       # README + 6 agents + 3 skills
└── data/                          # 可跑通的示例数据(术语库/记忆/提交单/回流账本)
```
> 交付研发时:`agents/*.md` → `.claude/agents/`;`skills/*` → `.claude/skills/`;`data/` 接入 Starling 术语/记忆存储。
